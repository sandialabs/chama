name: test_env
channels:
    - defaults
    - http://conda.anaconda.org/pyomo
dependencies:
    - python=3.5
    - numpy
    - scipy
    - pandas
    - nose
    - matplotlib
    - pip:
        - coveralls